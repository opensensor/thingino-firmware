CONFIG_T32_VARIANT_NQ=y
# XTX flash support: the Wyze Cam Pan v4 (and other T32NQ boards) use an XTX
# XT25F128B SPI NOR (JEDEC 0b 40 18), which the stock isvp_t32_sfcnor defconfig
# does not enable, so U-Boot reports "unrecognized JEDEC id bytes: 0b, 40, 18".
CONFIG_SPI_FLASH_XTX=y
